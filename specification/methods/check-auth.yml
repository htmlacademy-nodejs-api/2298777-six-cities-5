tags:
  - auth
summary: Проверяет аутефикацию пользователя.
security:
  - bearerAuth:
      - read
      - write
responses:
  "200":
    description: Аутентификация прошла успешно.
    content:
      application/json:
        schema:
          $ref: ../schemas/get-user.yml
