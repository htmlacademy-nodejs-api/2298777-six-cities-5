tags:
  - rents
summary: Получение списка предложений
description: Получение порции списка предложений в порядке создания.
responses:
  "200":
    description: Список предложений получен
    content:
      application/json:
        schema:
          $ref: ../schemas/get-rents.yml
parameters:
  - in: query
    name: count
    required: false
    schema:
      type:
        number
